services:
  - type: web
    name: parclick-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: gunicorn server:app --timeout 200 --workers 1
    envVars:
      PYTHON_VERSION: 3.11.9
